- var docs = model.get('docs');

.track-list
  ul
    foreach docs as item
      li(id=item._id)
        .swipeEl
          .track-title=item.Title
          .more
            =item.Artist + " - " + item.Album
          .swipeCover
            h1 Undo
